name: Lint

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        lint-task:
          - eslint
          - prettier
          - typescript

    steps:
      - uses: actions/checkout@v3
      - name: Install
        run: npm ci
      - name: Run linting task
        run: npm run lint:${{ matrix.lint-task }}
